
# Define target name
set (TARGET_NAME imgui)

# Define source files
file (GLOB CPP_FILES *.cpp)
file (GLOB H_FILES *.h)
set (SOURCE_FILES ${CPP_FILES} ${H_FILES})

set (INCLUDE_DIRS_ONLY ${TPT_ROOT}/imgui)

# Setup target
setup_library ()
